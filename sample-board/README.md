# sample-board

A fixture board mirroring the `.claude/tasks/` schema (see KICKOFF.md). Used to develop and demo the
Kanban UI and to test the board parser with no GitHub access. Every status is populated, including an
`awaiting-go-no-go` idea (0001) and a ship-ready `done` task (0005).
